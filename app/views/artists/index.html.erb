<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Artists</title>
  </head>
  <body>
    <% @artists.each do |artist| %>
      <%= link_to "#{artist.name}" %>
    <% end %>
  </body>
</html>
