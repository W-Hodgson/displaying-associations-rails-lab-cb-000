<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Artists</title>
  </head>
  <body>
    <% @artists.each do |artist| %>
      <%= link_to "Artist Name: #{artist.name}  Song Count #{artist.song_count}"%>
    <% end %>
  </body>
</html>
