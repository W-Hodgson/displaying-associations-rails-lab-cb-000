<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Song Index</title>
  </head>
  <body>
    <% @songs.each do |song| %>
      <%= link_to song.artist_name %>
      <p><%= song.artist_name %> <%= song.title %></p>
    <% end %>
  </body>
</html>
